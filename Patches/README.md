# MAGES game patches

This tree contains game-specific patch sources and maintenance metadata. Shared tooling such as EasyPatcher and MagesLib remains at repository level and is not vendored into each game.

Layout: `Patches/<Series>/<Game>/<Locale>/`. Each game/locale directory owns its patch data, maintenance scripts, authority metadata, documentation, and milestone evidence.
